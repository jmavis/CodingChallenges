#include <stdio.h>
#include <iostream>
#include <stdlib.h> 
#include <string>
#include <fstream>

using namespace std;
/*
void ParseLine(string line);
int SumOfDigits(string number);

/// Author Jared Mavis
/// Username jmavis
/// CodeEval Sum of Digits
int main (int argc, char* argv[]){
	ifstream file;
	file.open(argv[1]);
	string lineBuffer;
	while (!file.eof()) {
		getline(file, lineBuffer);
		if (lineBuffer.length() == 0){
			continue; //ignore all empty lines
		} else {
			ParseLine(lineBuffer);
		}
	}
	cin >> lineBuffer;
}

/// Will print out the result of SumOfDigits on the line
void ParseLine(string line){
	cout << SumOfDigits(line) << endl;
}

/// Will find the sum of the given number
int SumOfDigits(string number){
	int sum = 0;
	for (int i = 0; i < number.length(); i++){
		sum += (number[i] - '0');
	}
	return (sum);
}*/